<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('sales_index') }}">BoVoyage</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% if client is defined and client is not null %}
                {% if client.role == 1 %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('travel_index') }}">Voyage Admin <span class="sr-only">(current)</span></a>
                    </li>
                {% endif %}
            {%  else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('sales_index') }}">Voyages Visiteur</a>
                    </li>
            {% endif %}
            <li class="nav-item active">
                {% if client is defined and client is not null %}
                    <a class="nav-link" href="{{ path('logout') }}">Deconnexion</a>
                {% else %}
                    <a class="nav-link" href="{{ path('login') }}">Connexion</a>
                {% endif %}
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search" placeholder="Rechercher">
            <button class="btn btn-primary border my-2 my-sm-0" type="submit">Rechercher</button>
        </form>
    </div>
</nav>